<h1>新規投稿</h1>
<!--新規投稿用のフォームを作成するときに使うform_with機能-->
<%= form_with model:@list,url:'/todolists',local:true do |f| %>
<!--form_with：railsで情報を送信するための動的テンプレート-->
   <!--インスタンス変数＠list（List.new)を取得し、
    ブロック変数f（do~endまで）に渡してる-->
   <!--local:trueは非同期通信（待たずに処理）を行わないために必要な記述-->
<h4>タイトル</h4>
<%= f.text_field :title %>
<h4>本文</h4>
<%= f.text_area :body %>
<h4>画像</h4>
<%= f.attachment_field :image %>
<%= f.submit '投稿' %>
<% end %>